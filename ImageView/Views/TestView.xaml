<UserControl x:Class="ImageView.Views.TestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:flux="clr-namespace:WickedNite.Flux;assembly=WickedNite.Flux"
    Height="300" Width="300">
    <Grid>
        <TextBox Height="23" Margin="12,12,93,0" Name="textBox1" VerticalAlignment="Top" Text="{Binding Name}" />
        <Button Height="23" HorizontalAlignment="Right" Margin="0,12,12,0" Name="button1" 
                VerticalAlignment="Top" Width="75" Content="Go!" Command="{flux:Action Go}" />
    </Grid>
</UserControl>
